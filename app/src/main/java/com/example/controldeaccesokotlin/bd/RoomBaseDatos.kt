package com.example.controldeaccesokotlin.bd
/*
import androidx.room.Database
import androidx.room.RoomDatabase
import com.example.controldeaccesokotlin.dao.DaoHistorial

@Database(
    entities = [ModeloHistorial::class /*, poner todas la entidades */],
    version = 1
)

abstract class RoomBaseDatos : RoomDatabase() {

    // Poner todos los DAO (Por cada entidad/Modelo(dataclase))

    abstract val daoHistorial : DaoHistorial
}

*/